# 포트 8085 Docker 연결 상태 확인 보고서

> **확인일**: 2025-01-09  
> **포트**: 8085

---

## 📊 확인 결과

### 포트 8085 사용 프로세스

**프로세스 정보**:
- **PID**: 3651153
- **프로세스**: `java -Xmx512m -jar /app/batch.jar`
- **사용자**: root
- **상태**: 실행 중 (Ssl)
- **시작 시간**: Jan09 (약 4시간 22분 실행 중)

### Docker 컨테이너 확인

**결과**: ❌ **Docker 컨테이너에서 실행되지 않음**

- Docker 컨테이너 목록에 batch 관련 컨테이너 없음
- 모든 Docker 컨테이너의 포트 매핑에 8085 없음
- 프로세스는 호스트에서 직접 실행 중

### 포트 상태

```
tcp6       0      0 :::8085                 :::*                    LISTEN      3651153/java
```

- 포트 8085는 호스트에서 직접 LISTEN 중
- Docker 포트 포워딩이 아님
- 호스트의 Java 프로세스가 직접 포트 사용

---

## 🔍 분석

### 프로세스 경로 분석

**경로**: `/app/batch.jar`

이 경로는:
- ❌ Docker 컨테이너 내부 경로처럼 보이지만
- ✅ 실제로는 호스트에서 실행 중인 프로세스
- ✅ Docker 컨테이너가 아닌 호스트의 Java 프로세스

### 실행 환경

- **실행 위치**: 호스트 (EC2 인스턴스)
- **Docker 연결**: 없음
- **포트 바인딩**: 호스트에 직접 바인딩

---

## 🔧 해결 방법

### 방법 1: 기존 프로세스 중지 후 재실행

```bash
# 프로세스 중지
sudo kill 3651153

# 또는 강제 종료
sudo kill -9 3651153

# 재실행
bash /home/ec2-user/backups/archive-chatrooms.sh
```

### 방법 2: 포트 변경 (권장)

`application.yml`에서 포트를 변경:

```yaml
server:
  port: 0  # 랜덤 포트 (충돌 방지)
```

### 방법 3: 웹 서버 비활성화

```yaml
spring:
  main:
    web-application-type: none  # 웹 서버 비활성화
```

---

## 📝 결론

### ✅ 확인 완료

- **Docker 연결**: ❌ 없음
- **포트 8085**: ✅ 호스트에서 직접 사용 중 (PID: 3651153)
- **프로세스**: 호스트의 Java 프로세스
- **해결 필요**: 포트 충돌 해결 필요

---

**상태**: ✅ **포트 8085는 Docker가 아닌 호스트에서 직접 사용 중**

