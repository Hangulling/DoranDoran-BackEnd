<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    
    <springProperty scope="context" name="springAppName" source="spring.application.name"/>
    <springProperty scope="context" name="serviceName" source="spring.application.name" defaultValue="api-gateway"/>
    
    <!-- JSON 로그 포맷 (프로덕션) -->
    <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <timeZone>Asia/Seoul</timeZone>
                </timestamp>
                <version/>
                <logLevel/>
                <message/>
                <loggerName/>
                <pattern>
                    <pattern>
                        {
                            "service": "${serviceName}",
                            "thread": "%thread",
                            "class": "%logger{36}"
                        }
                    </pattern>
                </pattern>
                <stackTrace/>
                <mdc/>
            </providers>
        </encoder>
    </appender>
    
    <!-- 일반 텍스트 로그 포맷 (개발 환경) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 프로파일별 설정 -->
    <springProfile name="local,dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.dorandoran" level="DEBUG"/>
    </springProfile>
    
    <springProfile name="prod,docker">
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
        <logger name="com.dorandoran" level="INFO"/>
        <logger name="org.springframework" level="WARN"/>
        <logger name="reactor.netty" level="WARN"/>
    </springProfile>
    
    <!-- 기본 설정 (프로파일이 없을 때) -->
    <springProfile name="!local,!dev,!prod,!docker">
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>
</configuration>

