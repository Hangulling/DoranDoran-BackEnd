spring:
  application:
    name: user-service-test
  
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# HMAC 설정
gateway:
  jwt:
    hmac-secret: test-hmac-secret-for-testing
    skew-ms: 60000

# 로깅 설정
logging:
  level:
    com.dorandoran.user: DEBUG
    org.springframework.web.servlet: DEBUG