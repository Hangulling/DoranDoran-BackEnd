# Store 서비스 병목 현상 체크 결과

**체크 시간**: 2025-11-04 16:24:44 KST  
**서비스**: dorandoran-store (포트 8084)  
**체크 대상 엔드포인트**: `/api/store/bookmarks`, `/api/store/bookmarks/cursor`

---

## 종합 판단

**현재 상태: ✅ 정상 운영 중 (우수한 성능)**

### 전체 지표 요약

| 항목 | 결과 | 상태 | 해석 |
|------|------|------|------|
| **서비스 상태** | ✅ 정상 작동 중 | 정상 | 모든 컴포넌트 정상 |
| **응답 시간** | 평균 0.044초 | ✅ 우수 | 매우 빠름 (1.2초 미만) |
| **연결 풀** | 0/20 (0% 사용률) | 정상 | 연결 풀 여유 있음 |
| **Circuit Breaker** | 메트릭 없음 | - | Resilience4j 메트릭 미수집 또는 미설정 |
| **힙 메모리** | 1.02MB / 957.99MB (1%) | ⚠️ 의심 | 계산 오류 가능성 (너무 낮음) |
| **에러 발생** | 1건 (5분간) | ✅ 정상 | 임계값(10건) 이하 |

---

## 상세 분석

### 1. 서비스 상태 ✅

**결과**: ✅ 서비스가 정상 작동 중입니다

**상세**:
- 모든 컴포넌트가 UP 상태
- 데이터베이스 연결 정상
- 서비스가 정상적으로 시작되어 실행 중

**의미**: 서비스가 정상적으로 시작되었고 모든 의존성에 정상적으로 연결되어 있습니다.

---

### 2. 응답 시간 체크 ✅

**결과**: ✅ 우수 (평균 0.044초)

**엔드포인트별 상세**:
```
/api/store/bookmarks: 평균 0.025초 (요청 수: 27건)
/api/store/bookmarks/cursor: 평균 0.049초 (요청 수: 122건)

전체 평균 응답 시간: 0.044초 (총 요청 수: 149건, 엔드포인트: 2개)
```

**분석**:
- ✅ 두 엔드포인트 모두 매우 빠름 (0.025초, 0.049초)
- ✅ 전체 평균 0.044초로 매우 우수한 성능
- ✅ 1.2초 임계값보다 훨씬 낮음 (약 27배 빠름)
- ✅ **실제 트래픽이 있어 데이터 수집됨** (총 149건 요청)

**의미**: 
- 응답 시간이 매우 빠르고 병목 현상이 발생하지 않았습니다.
- 실제 사용자가 서비스를 이용하고 있어서 의미 있는 데이터가 수집되었습니다.
- Store 서비스는 현재 모든 서비스 중 가장 빠른 응답 시간을 보이고 있습니다.

**비교**:
- Auth 서비스: 평균 0.082초 (130건)
- Store 서비스: 평균 0.044초 (149건) ← **가장 빠름**

---

### 3. 데이터베이스 연결 풀 체크 ✅

**결과**: 정상 (사용률 0%)

**상세**:
```
활성 연결: 0.0 / 최대: 20.0 (사용률: 0%)
대기 중인 연결 요청: 없음
```

**분석**:
- 연결 풀 사용률이 0%로 매우 여유 있음
- 최대 20개 연결 중 0개만 사용 중
- 대기 중인 연결 요청 없음

**의미**: 
- 데이터베이스 연결이 충분히 여유 있어서 병목 현상이 발생하지 않습니다.
- 다른 서비스에 비해 연결 풀 크기가 작지만(최대 20개), 현재 사용률이 0%이므로 충분합니다.

**비교**:
- Auth: 최대 30개 연결
- Chat: 최대 50개 연결
- Store: 최대 20개 연결

---

### 4. Circuit Breaker 상태 ⚠️

**결과**: 메트릭 데이터를 가져올 수 없습니다

**상세**:
- Resilience4j Circuit Breaker 메트릭이 Actuator에서 노출되지 않음
- 또는 Store 서비스에 Circuit Breaker가 설정되지 않았을 수 있음

**분석**:
- Store 서비스는 다른 서비스 호출이 적을 수 있음
- Circuit Breaker가 필요하지 않거나, 설정되어 있지 않을 수 있음

**의미**: 
- Circuit Breaker 메트릭을 확인할 수 없어서 상태를 판단할 수 없습니다.
- 하지만 현재 에러율이 매우 낮으므로(1건) 큰 문제는 없어 보입니다.

---

### 5. 힙 메모리 사용률 ⚠️

**결과**: 의심 (사용률 1% - 비정상적으로 낮음)

**상세**:
```
힙 메모리: 1.02483969MB / 957.99MB (사용률: 1.02483969%)
```

**분석**:
- 힙 메모리 사용률이 1%로 비정상적으로 낮음
- 메트릭 계산 오류 가능성이 높음
- 실제로는 더 높을 가능성이 있음 (일반적으로 10-20%는 사용)

**의미**: 
- 메트릭 계산에 문제가 있을 수 있습니다.
- 실제 메모리 사용률은 정상 범위일 가능성이 높지만, 정확한 값은 확인이 필요합니다.

**권장사항**: 
- 메트릭 수집 확인
- 또는 다른 방법으로 메모리 사용률 확인

---

### 6. 에러 발생 ✅

**결과**: 정상 (1건, 임계값 10건 이하)

**상세**:
- 최근 5분간 에러 발생 건수: **1건**
- 임계값: 10건 (이하)

**에러 내용**:
```
DuplicateBookmarkException: 이미 보관함에 저장된 표현입니다
중복 저장 시도: userId=..., messageId=...
```

**분석**:
- 정상적인 비즈니스 로직 에러 (중복 저장 시도)
- 사용자가 이미 저장된 표현을 다시 저장하려고 시도한 경우
- 실제 서비스 장애가 아님
- 에러 핸들링이 정상적으로 작동하고 있음

**의미**: 
- 에러 발생 빈도가 매우 낮아서 정상 범위입니다.
- 실제 장애가 아니라 정상적인 사용 패턴에서 발생하는 에러입니다.

**비교**:
- Auth: 24건 (경고)
- Chat: 259건 (심각)
- Store: 1건 (정상) ← **가장 낮음**

---

### 7. 최근 로그 요약

**최근 활동**:
- 정상적인 쿼리 실행
- 중복 저장 시도 경고 (정상적인 비즈니스 로직)
- 에러 핸들링 정상 작동

**의미**: 
- 서비스가 정상적으로 요청을 처리하고 있습니다.
- 비즈니스 로직이 정상적으로 작동하고 있습니다.

---

## 종합 판단

### ✅ 긍정적 지표

1. **응답 시간 우수**: 평균 0.044초로 모든 서비스 중 가장 빠름
2. **에러율 매우 낮음**: 5분간 1건으로 정상 범위
3. **리소스 여유**: 연결 풀, 메모리(계산 오류 제외) 모두 여유 있음
4. **실제 트래픽**: 체크 대상 엔드포인트에 실제 요청이 있어 의미 있는 데이터 수집

### ⚠️ 주의사항

1. **힙 메모리 메트릭**: 계산 오류 가능성 (1%는 비정상적으로 낮음)
2. **Circuit Breaker 메트릭**: 확인 불가

### 📊 성능 지표 요약

- **평균 응답 시간**: 0.044초 (목표: < 1.2초) ✅ **우수**
- **연결 풀 사용률**: 0% (목표: < 90%) ✅
- **힙 메모리 사용률**: 1%? (목표: < 85%) ⚠️ 계산 오류 가능성
- **Circuit Breaker**: 확인 불가 ⚠️
- **에러율**: 1건/5분 (목표: < 10건) ✅ **정상**

---

## 서비스별 비교

| 서비스 | 평균 응답 시간 | 요청 수 | 연결 풀 | 메모리 | 에러율 |
|--------|---------------|---------|---------|--------|--------|
| **Auth** | 0.082초 | 130건 | 0/30 (0%) | 16% | 24건 |
| **Chat** | 데이터 없음 | 0건 | 0/50 (0%) | 17% | 259건 |
| **Store** | **0.044초** | **149건** | 0/20 (0%) | 1%? | **1건** |

**Store 서비스가 가장 우수한 성능을 보이고 있습니다**:
- ✅ 가장 빠른 응답 시간 (0.044초)
- ✅ 가장 낮은 에러율 (1건)
- ✅ 실제 트래픽이 있어 의미 있는 데이터 수집

---

## 권장사항

### 즉시 조치

1. **힙 메모리 메트릭 확인**: 
   - 계산 오류 가능성이 있으므로 메트릭 수집 확인
   - 또는 다른 방법으로 실제 메모리 사용률 확인

2. **Circuit Breaker 메트릭 노출** (필요 시):
   - Actuator 설정에서 Resilience4j 메트릭 노출 확인

### 모니터링 계획

1. **주기적 체크**: 
   ```bash
   watch -n 30 ~/check_bottleneck.sh dorandoran-store 8084
   ```

2. **트래픽 증가 시**: 
   - 실제 트래픽 발생 시 응답 시간과 연결 풀 사용률 추이 확인
   - 피크 시간대 모니터링 강화

3. **성능 유지**: 
   - 현재 우수한 성능을 유지할 수 있도록 주기적 모니터링

### 예방 조치

1. **연결 풀 모니터링**: 사용률이 80% 이상으로 증가하면 연결 풀 크기 검토
2. **응답 시간 모니터링**: 평균 응답 시간이 0.1초 이상으로 증가하면 원인 분석
3. **에러율 모니터링**: 에러율이 증가하면 원인 분석

---

## 결론

**현재 Store 서비스는 정상 작동 중이며, 모든 서비스 중 가장 우수한 성능을 보이고 있습니다.**

- ✅ 모든 핵심 지표가 정상 범위 내
- ✅ 응답 시간이 매우 우수함 (0.044초)
- ✅ 에러율이 매우 낮음 (1건)
- ✅ 실제 트래픽이 있어 의미 있는 데이터 수집
- ⚠️ 힙 메모리 메트릭 계산 오류 가능성
- ⚠️ Circuit Breaker 메트릭 확인 불가

**다음 체크 시점**: 
- 트래픽이 증가할 때
- 또는 주기적으로 (예: 30분마다)

---

## 체크 스크립트 실행 명령어

```bash
# 기본 체크
~/check_bottleneck.sh dorandoran-store 8084

# 주기적 모니터링 (30초마다)
watch -n 30 ~/check_bottleneck.sh dorandoran-store 8084

# 결과를 파일로 저장
~/check_bottleneck.sh dorandoran-store 8084 > store_check_$(date +%Y%m%d_%H%M%S).log 2>&1
```

---

**생성일**: 2025-11-04  
**체크 스크립트 버전**: check_bottleneck.sh (서비스별 엔드포인트 지원)

