# 사용자 요청 분석 보고서

**작성일**: 2025년 11월 12일  
**사용자 ID**: `54ae36f4-77b3-44d1-ac42-0ea82ccd1807`  
**분석 기간**: 최근 168시간 (7일)

---

## 1. 요약

### 1.1 데이터 불일치 발견

**문제점**: 
- 이전 분석에서 "채팅 요청 62건"으로 보고되었으나
- 실제 라우팅된 요청은 **15건**에 불과함
- 데이터베이스의 실제 메시지 수는 **2건**

**원인 분석**:
- 이전 스크립트가 `X-User-Id` 헤더가 있는 **모든 로그 라인**을 카운트
- 실제로는 `Mapping [Exchange:` 패턴이 실제 라우팅된 요청만 의미
- Gateway 로그에는 하나의 요청에 대해 여러 라인(헤더, 관찰, 응답 등)이 기록됨

---

## 2. 실제 요청 분석

### 2.1 전체 요청 수

**실제 라우팅된 요청**: 15건

### 2.2 API별 요청 분류

| API 경로 | HTTP 메서드 | 요청 수 |
|----------|------------|--------|
| `/api/chat/**` | GET | 9건 |
| `/api/chat/**` | POST | 2건 |
| `/api/users/**` | GET | 4건 |

**총 채팅 관련 요청**: 11건 (GET 9건 + POST 2건)

### 2.3 채팅 관련 요청 상세

#### GET 요청 (9건)
- `/api/chat/chatrooms/last-interactions` - 채팅방 목록 조회
- `/api/chat/chatrooms/{id}/messages` - 메시지 목록 조회
- `/api/chat/stream/{id}` - SSE 스트림 연결

#### POST 요청 (2건)
- `/api/chat/chatrooms` - 채팅방 생성
- `/api/chat/chatrooms/{id}/leave` - 채팅방 나가기

**메시지 생성 요청 (POST /api/chat/chatrooms/{id}/messages)**: 0건

### 2.4 요청 타입별 통계

- **GET**: 13건
- **POST**: 2건

### 2.5 시간대별 분포

- **01시**: 15건 (모든 요청이 2025-11-12 01시에 집중)

---

## 3. 데이터베이스 vs 로그 비교

### 3.1 데이터베이스 기록

- **총 메시지 수**: 2건
  - 봇 메시지: 1건
  - 시스템 메시지: 1건
  - 사용자 메시지: 0건

### 3.2 Gateway 로그 기록

- **채팅 관련 요청**: 11건
- **메시지 생성 요청**: 0건

### 3.3 분석 결과

**일치하는 부분**:
- ✅ 메시지 생성 요청이 0건 → 데이터베이스에 사용자 메시지가 0건인 것과 일치
- ✅ 채팅방 생성 요청이 있음 → 데이터베이스에 채팅방이 1개 있는 것과 일치

**설명 가능한 부분**:
- 채팅방 목록 조회, 메시지 목록 조회, SSE 연결 등은 메시지를 생성하지 않으므로 데이터베이스에 기록되지 않음
- 봇 메시지와 시스템 메시지는 채팅방 생성 시 자동으로 생성되므로 별도의 POST 요청이 없음

---

## 4. 이전 분석 스크립트의 문제점

### 4.1 문제점

**이전 스크립트 (`traffic-analysis.sh`)**:
```bash
# X-User-Id 헤더가 있는 모든 라인을 카운트
grep -E "X-User-Id.*chat|/api/chat.*X-User-Id"
```

**문제**:
- 하나의 요청에 대해 여러 로그 라인이 생성됨:
  - `Mapping [Exchange: ...]` - 실제 라우팅
  - `Will instrument the HTTP request headers ...` - 헤더 로깅
  - `Client observation ...` - 관찰 로깅
  - 각 라인에 `X-User-Id` 헤더가 포함되어 중복 카운트됨

### 4.2 해결 방법

**개선된 스크립트 (`analyze-user-requests.sh`)**:
```bash
# 실제 라우팅된 요청만 카운트
grep "Mapping \[Exchange:"
```

**결과**:
- 실제 요청 수만 정확하게 집계
- API별, 메서드별 분류 가능

---

## 5. 결론

### 5.1 실제 사용자 활동

**사용자 `54ae36f4-77b3-44d1-ac42-0ea82ccd1807`의 실제 활동**:

1. **2025-11-12 01:36:22**: 로그인
2. **2025-11-12 01:36:23**: 
   - 사용자 정보 조회 (`/api/auth/me`)
   - 채팅방 목록 조회 (`/api/chat/chatrooms/last-interactions`)
3. **2025-11-12 01:36:47**: 새 채팅방 생성 (`POST /api/chat/chatrooms`)
4. **2025-11-12 01:36:49**: 
   - 메시지 목록 조회 (`/api/chat/chatrooms/{id}/messages`)
   - SSE 스트림 연결 (`/api/chat/stream/{id}`)
   - 북마크 조회 (`/api/store/bookmarks/chatroom/{id}`)
5. **2025-11-12 01:37:04**: 메시지 전송 시도 (하지만 실제로는 메시지가 생성되지 않음)
6. **2025-11-12 01:37:12**: 채팅방 나가기 (`POST /api/chat/chatrooms/{id}/leave`)

### 5.2 요청 수 정정

- ❌ **이전 보고**: 채팅 요청 62건
- ✅ **실제**: 채팅 관련 요청 11건 (전체 요청 15건)
- ✅ **데이터베이스**: 메시지 2건 (봇 1건 + 시스템 1건)

### 5.3 권장 사항

1. **분석 스크립트 개선**: `Mapping [Exchange:` 패턴만 사용하여 실제 요청만 집계
2. **로그 분석 정확성**: 하나의 요청이 여러 로그 라인을 생성할 수 있음을 고려
3. **메트릭 수집**: Prometheus 등 메트릭 시스템 활용 권장

---

**작성자**: AI Assistant  
**작성일**: 2025년 11월 12일

